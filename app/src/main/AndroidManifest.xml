<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Mapa y comparticion de posicion en campo (fase 1 del port Falcon One). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Livestream del SOS: el emisor publica su camara y voz al canal. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Control de la bodycam W1 por Bluetooth RFCOMM (fase 4 del port).
         El foreground service mantiene el enlace vivo en segundo plano. -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <!-- cancelDiscovery() exige ADMIN hasta Android 11 y SCAN desde Android 12.
         neverForLocation: no usamos el escaneo para deducir ubicacion. -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <!-- Radio tactica siempre a la escucha: el PTT tiene que sonar con la app
         cerrada. specialUse en vez de mediaPlayback porque desde Android 15 un
         BOOT_COMPLETED no puede levantar mediaPlayback; ver RadioService. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Sin este permiso el servicio corre igual pero su notificacion no se ve, y
         es la unica senal de que el telefono sigue en el canal. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- usesCleartextTraffic: el servidor HTTP de la bodycam W1 (visor remoto
         y descarga de grabaciones) es http:// en la red WiFi local; sin esto
         Android bloquea la conexion. El trafico a internet sigue siendo TLS. -->
    <application
        android:name=".AeriaNexusApp"
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AeriaNexusPrototype">
        <!-- Permite que la app de camara del sistema escriba fotos y videos
             directamente en nuestra carpeta local de evidencia. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Sostiene el enlace BT con la bodycam cuando la app va a segundo
             plano; lo arranca y detiene BodycamRepository. -->
        <service
            android:name=".BodycamService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- Sostiene la radio tactica (canal de Agora) cuando la app no esta
             delante; lo arranca y para AeriaNexusApp segun el estado de sesion.
             PENDIENTE: para poder HABLAR con la app cerrada habria que anadir
             android:foregroundServiceType="specialUse|microphone" y levantarlo con
             los dos tipos solo desde primer plano — `microphone` no se puede
             arrancar ni desde boot ni desde background. Ver RadioService. -->
        <service
            android:name=".RadioService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Radio tactica push-to-talk entre agentes: el telefono permanece en el canal de voz para recibir transmisiones de otras unidades con la app cerrada" />
        </service>

        <!-- Despierta el proceso tras un reinicio para retomar subidas y volver al
             canal. La radio no arranca aqui: la sesion vuelve a LOCKED. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AeriaNexusPrototype">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>